# =========================================================
# CRM INSIGHTS
# Variables de entorno de ejemplo
# =========================================================

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# IA (Gemini)
# NOTA: esta key se usa directamente desde el frontend (decision
# tomada para esta etapa del proyecto). Queda visible en el bundle
# del navegador. Cuando se agregue un backend propio (Edge Function),
# esta llamada deberia moverse ahi y dejar de exponerse aqui.
VITE_GEMINI_API_KEY=
